---
const { title } = Astro.props;
---
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title} | ΣTRATOS Consulting</title>
    </head>
  <body>
    <slot /> </body>
</html>

<style is:global>
  /* :root や共通の CSS はここに移動 */
  :root { --navy: #001f3f; --accent: #00d4ff; }
  body { margin: 0; font-family: 'Inter', sans-serif; }
</style>