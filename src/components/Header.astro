<header class="header">
  <div class="header-container">
    <a href="/" class="logo">
      <span class="sigma">Σ</span>TRATOS <span class="consulting">Consulting</span>
    </a>
    
    <nav class="nav">
      <div class="nav-links">
        <a href="#services">Services</a>
        <a href="#cases">Case Studies</a>
        <a href="#company">Company</a>
      </div>
      <a href="#contact" class="btn-contact">Contact Us</a>
    </nav>
  </div>
</header>

<style>
  /* 共通変数が効いていない場合を想定して、直接色を指定します */
  .header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 80px;
    background-color: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid rgba(0, 31, 63, 0.1);
    display: flex;
    align-items: center;
    z-index: 1000;
  }

  .header-container {
    max-width: 1200px;
    width: 100%;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .logo {
    text-decoration: none;
    color: #001f3f; /* ネイビー */
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    display: flex;
    align-items: center;
    font-family: 'Inter', sans-serif;
  }

  .sigma {
    color: #00d4ff; /* アクセントブルー */
    font-size: 1.8rem;
    margin-right: 4px;
  }

  .consulting {
    font-size: 0.75rem;
    font-weight: 400;
    margin-left: 8px;
    opacity: 0.6;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 3rem;
  }

  .nav-links {
    display: flex;
    gap: 2.5rem;
  }

  .nav-links a {
    text-decoration: none;
    color: #001f3f;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    transition: color 0.3s ease;
  }

  .nav-links a:hover {
    color: #00d4ff;
  }

  .btn-contact {
    text-decoration: none;
    color: #001f3f;
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.7rem 1.8rem;
    border: 1px solid #001f3f;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .btn-contact:hover {
    background-color: #001f3f;
    color: #ffffff;
  }

  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }
  }
</style>